// $(document).ready(function() {
  // var maxScrollTop, next_page_url, timer;
  // var table = $('#innertable');
  // maxScrollTop = function() {
    // return table[0].scrollHeight - table.outerHeight();
    // console.log(table);
  // };
  // if ($('.pagination').size() > 0) {
    // $('.pagination').hide();
    // next_page_url = $('.pagination a[rel="next"]').attr('href');
    // $.getScript(next_page_url);
    // $('#innertable').on('scroll', function() {
    // console.log($('#innertable').scrollTop());
      // if(timer) {
      	// clearTimeout(timer);
      // }
      // timer = setTimeout(function() {	
	      // var next_next_page_url = $('.pagination a[rel="next"]').attr('href');
	      // if (next_next_page_url && $('#innertable').scrollTop() > maxScrollTop() - 15) {
	        // $.getScript(next_next_page_url);
	        // $('#loader').show();
      		// $('#loader').fadeOut(3500);
	   		// }
	   // }, 1001);
    // });
	// }
// });
// 
